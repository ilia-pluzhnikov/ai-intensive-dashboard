# AI INTENSIVE — Student Dashboard

> **ВАЖНО:** Любые изменения в этом файле должны также переноситься в `AGENTS.md` и `GEMINI.md`.

## О проекте

Геймифицированный дашборд для отслеживания прогресса учеников AI-практикума.
Визуальный стиль: Far Cry 3: Blood Dragon (неон, ретро-футуризм, пиксель-арт).

## Календарь — Поток #6 (Июнь–Июль 2026)

**Даты потока:** 30 июня — 17 июля 2026 (+ подготовительная неделя 23–28 июня)
**Расписание созвонов:** Вторник + Пятница, 18:00 МСК, 1.5–2 часа
**Подготовительная неделя:** 23–28 июня — буферная настройка (Claude + оплата для РФ)

| Созвон | Дата | День | Тема |
|--------|------|------|------|
| 1 | 30 июня | Вторник | Обзор AI-рынка (почему Claude), выбор проекта |
| 2 | 3 июля | Пятница | Среды работы (Web/Desktop/Terminal), структура проекта |
| 3 | 7 июля | Вторник | Деплой, Git, лайфхаки моделей, команды Claude Code |
| 4 | 10 июля | Пятница | Skills, субагенты, MCP, экономика агентов |
| 5 | 14 июля | Вторник | Context Engineering: слои контекста, поверхность внимания, экономика кэша |
| 6 | 17 июля | Пятница | Демо-день: анатомия агента, 5 слоёв памяти, проактивность |

> **Бонусные лекции:** 21 июля (Вт) — Проактивный агент на сервере · 24 июля (Пт) — Юзкейсы, голос, база знаний.

> **Историческое:** Поток #3 (17 марта — 3 апреля 2026) → `data/cohort-3-archive.json` (коммит 298564f). Поток #4 (14 — 30 апреля 2026) → `data/cohort-4-archive.json` (коммит af97c9f). Поток #5 (26 мая — 12 июня 2026) → `data/cohort-5-archive.json` (коммит a080348).

## Метафора

- Ученики — кибер-агенты, убегающие от Blood Dragon
- Дракон движется автоматически (время идёт)
- Ученики двигаются когда выполняют задания
- Отстал — дракон догоняет, аватар в опасности

## Структура

```
├── index.html                # Публичный дашборд
├── admin-x-y-z.html          # Админка для чекинов
├── data/
│   └── cohort-1.json         # Данные потока (ученики, задания, прогресс)
├── css/
│   └── blood-dragon.css
├── js/
│   ├── dashboard.js
│   └── admin.js
├── netlify/
│   └── functions/
│       ├── get-data.js       # GET /api/data — чтение из Netlify Blobs
│       ├── set-data.js       # POST /api/save — запись в Netlify Blobs
│       └── init-data.js      # GET /api/init — seed начальных данных
└── assets/
    ├── avatars/              # Аватары учеников
    └── *.png                 # Спрайты дракона
```

## Данные

JSON-файл `data/cohort-1.json` содержит:
- `cohort` — название потока
- `startDate`, `endDate` — даты потока (для позиции дракона)
- `students` — список учеников с аватарами
- `weeks[].sections[].tasks` — задания по неделям и секциям с очками
- `checkins` — выполненные задания по ученикам (объект: studentId → [taskId, ...])

**Конвенции задач:**
- `"optional": true` — необязательная задача (бонус без штрафа). Не входит в `getMaxPoints()` (пропуск не снижает процент), но даёт очки, если выполнена. На дашборде — бейдж «опционально», позиция аватара клампится на 100% (`Math.min` в `getStudentPosition`). Логика в `js/dashboard.js` и `js/admin.js`.
- `★` в начале `title` — «ключевая веха фазы» (косметический маркер, ортогонален `optional`). Сейчас на 6 задачах: идея, CLAUDE.md, GitHub, свой Skill, деплой, демо. JS его не обрабатывает (просто символ в строке) — **не удалять при правке заголовков.**

## Редактирование

1. Открыть `/admin-x-y-z.html`
2. Ввести пароль
3. Кликать по ячейкам для toggle чекинов
4. Сохранить → данные отправляются в Netlify Blobs через `/api/save`

## Состояния аватаров

- **Fresh** (зелёный) — дракон далеко
- **Stressed** (оранжевый) — дракон приближается
- **Bitten** (красный) — дракон догнал
- **Leader** (золотой) — первое место
- **Dropped** (чёрный, 💀) — ученик выбыл (`"dropped": true` в данных)

## Деплой

- Хостинг: Netlify (статика + Functions)
- Хранилище данных: Netlify Blobs
- **Автодеплой из GitHub подключён** — `git push` в `main` триггерит билд автоматически
- Site ID: `424adeb3-79c9-472d-a0c8-b81fae93e54d`
- URL: https://ai-intensive-dashboard.netlify.app
- Fallback: если API недоступен, дашборд читает локальный `data/cohort-1.json`

## Два источника данных

Дашборд использует **два источника**, и это важно при обновлениях:

1. **Netlify Blobs** (основной) — `/api/data` читает, `/api/save` пишет. Админка работает с ним.
2. **`data/cohort-1.json`** (fallback) — используется если API недоступен.

**При изменении структуры данных** (задания, ученики, недели) нужно обновить оба:
1. Отредактировать `data/cohort-1.json`
2. Задеплоить статику: `netlify deploy --prod --dir=. --site=424adeb3-79c9-472d-a0c8-b81fae93e54d`
3. Залить данные в Blobs (см. ⚠️ ниже)

**Чекины** (галочки) обновляются только в Blobs через админку — в локальный JSON их переносить не нужно.

> ⚠️ **НЕ делай `curl -d @data/cohort-1.json`** напрямую в `/api/save` — локальный JSON содержит пустые чекины и затрёт реальные галочки из админки!
>
> Правильный порядок для шага 3:
> 1. Скачать текущие данные из Blobs: `curl -s https://ai-intensive-dashboard.netlify.app/api/data > /tmp/current.json`
> 2. Смержить структуру (weeks, students) из локального JSON с чекинами из Blobs
> 3. Отправить смерженный результат в `/api/save`
> 4. **Проверить с паузой ~3–4 с** (или cache-buster `?cb=<n>`): сразу после `POST` у Netlify Blobs есть короткое окно read-after-write — мгновенный `GET /api/data` может вернуть старые данные. Это не ошибка записи, а консистентность; повтор через пару секунд показывает актуальное.
>
> Пример на Python:
> ```python
> import json, urllib.request
> # Скачать текущие Blobs (с чекинами)
> blobs = json.loads(urllib.request.urlopen('https://ai-intensive-dashboard.netlify.app/api/data').read())
> # Прочитать локальный JSON (обновлённая структура)
> with open('data/cohort-1.json') as f: local = json.load(f)
> # Мерж: структура из локального, чекины из Blobs
> local['checkins'] = blobs['checkins']
> # Отправить
> req = urllib.request.Request('https://ai-intensive-dashboard.netlify.app/api/save',
>     data=json.dumps(local, ensure_ascii=False).encode('utf-8'),
>     headers={'Content-Type': 'application/json'}, method='POST')
> urllib.request.urlopen(req)
> ```
